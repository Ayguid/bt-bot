const config = {
    isRunning: false,
    delay: 1000,
    saveData: false,
    alertCooldown: 10 * 60 * 1000, // 10 minutes in milliseconds
    debug: false,
    telegramBotEnabled: true,
    klinesInterval: '2h'   
};

module.exports = config;